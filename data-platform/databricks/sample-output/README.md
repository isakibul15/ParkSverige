# Sample Output

This directory stores locally generated sample outputs for the Stockholm rule pipeline simulation.

Generate files with:

```bash
npm run stockholm:simulate -w @parksverige/workers
```

The generated files are meant to validate bronze, silver, and gold payload shapes before a real Databricks workspace is connected.

# Shared Config Package

This package is reserved for shared tooling configuration as the repo grows.

Expected future contents:

- ESLint presets
- Prettier config
- environment validation rules
- reusable tsconfig fragments

